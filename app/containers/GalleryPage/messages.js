/*
 * GalleryPage Messages
 *
 * This contains all the text for the GalleryPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GalleryPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GalleryPage container!',
  },
});
