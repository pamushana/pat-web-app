/*
 * CommuteRoutesPage Messages
 *
 * This contains all the text for the CommuteRoutesPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CommuteRoutesPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CommuteRoutesPage container!',
  },
});
