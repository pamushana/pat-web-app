/*
 * FleetPage Messages
 *
 * This contains all the text for the FleetPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.FleetPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the FleetPage container!',
  },
});
