/*
 * DivisionsPage Messages
 *
 * This contains all the text for the DivisionsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DivisionsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DivisionsPage container!',
  },
});
