/*
 * HirePage Messages
 *
 * This contains all the text for the HirePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HirePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HirePage container!',
  },
});
