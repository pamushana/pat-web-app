/*
 * PrePaidAccountPage Messages
 *
 * This contains all the text for the PrePaidAccountPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PrePaidAccountPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PrePaidAccountPage container!',
  },
});
