/*
 * CareersPage Messages
 *
 * This contains all the text for the CareersPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CareersPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CareersPage container!',
  },
});
