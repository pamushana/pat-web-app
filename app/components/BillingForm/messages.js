/*
 * BillingForm Messages
 *
 * This contains all the text for the BillingForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.BillingForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BillingForm component!',
  },
});
