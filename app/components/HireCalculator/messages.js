/*
 * HireCalculator Messages
 *
 * This contains all the text for the HireCalculator component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HireCalculator';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HireCalculator component!',
  },
});
